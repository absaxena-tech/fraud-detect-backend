# fraud-detect-backend
A fraud detection backend is a server-side system that analyzes transactions in real time to detect suspicious activity. It applies rules or ML models to generate a risk score and classify transactions as approved, flagged, or rejected, while storing data and exposing APIs for integration.
